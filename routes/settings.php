<?php

use Illuminate\Support\Facades\Route;

// Settings Routes
// These routes will be implemented in the future

Route::middleware(['auth', 'admin'])->prefix('settings')->name('settings.')->group(function () {
    // Placeholder for future settings routes
});

